# ALInvite 更新公告

## v2.0.2（2026-09-11）

> 适用 Minecraft 1.20.1 - 26.3，Java 17~21+（建议 21）
> 升级方式：替换 jar 后重启即可，配置自动合并、无需删除旧配置。

### 修复

- **MySQL 下唯一索引创建失败**：防重复计数使用的唯一索引此前使用了 SQLite 专属语法（`CREATE INDEX IF NOT EXISTS`），在 MySQL 上始终创建失败，跨服防重复计数处于失效状态。现按数据库类型分别处理，MySQL 启动时正常创建 `idx_alinvite_records_invitee_uniq` 唯一索引，跨服/并发下的邀请重复计数恢复数据库级防护。
- **启动横幅数据库类型显示错误**：横幅此前固定显示 SQLITE（读错了配置文件），现正确显示 database.yml 中配置的实际类型（MYSQL / SQLITE）。

### 调整

- **A「切换视图」按钮文案风格统一**：补充「视图 / 操作」段落标题、`▸` 信息行箭头与 `• 动作 ➤ 说明` 操作行格式，与全插件菜单文案风格一致；领取视图保留蓝色主题。
- **支持版本说明**更新为 `1.20.1 - 26.3`。

### 升级说明

- MySQL 服重启后若日志仍出现「创建邀请记录唯一索引失败」，说明历史数据中存在重复邀请记录（索引因数据冲突无法建立），需要人工清理重复数据后重启重建。
- 本次不涉及配置结构变更，`menus/`、`languages/` 的自定义内容均会保留。

---

## v2.0.1（2026-09-10）

- 返利记录菜单双视图（返利到账 ↔ 领取记录）：切换后记录条目与切换按钮整体换用对应视图样式。
- 领取动作落库：玩家手动领取、管理员核销均写入领取记录，领取视图不再为空。
- 新增管理员查看菜单：`/alinvite admin rebate <玩家>` 打开与玩家版同结构的记录界面（双视图 + 翻页），修复旧界面可拿走物品、关闭按钮无效、无翻页等问题。
- 实现 `record_retention_days`：返利/领取记录按保留天数自动清理（每 6 小时执行，0 = 永久保留）。
- 补齐缺失语言键（视图名、领取提示等），修复领取成功提示报错与视图名红字。
- 切换视图 / 翻页时清理残留条目；领取记录、管理员菜单文案中英双语。
